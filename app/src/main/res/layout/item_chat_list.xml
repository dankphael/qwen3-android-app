<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="@dimen/space_2"
    android:clickable="true"
    android:focusable="true"
    android:foreground="@drawable/bg_chat_list_item"
    app:cardElevation="0dp"
    app:cardBackgroundColor="@color/surface"
    app:cardCornerRadius="@dimen/space_12"
    app:strokeWidth="1dp"
    app:strokeColor="@color/outline_variant"
    app:contentPadding="@dimen/space_12">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Avatar placeholder -->
        <ImageView
            android:id="@+id/ivAvatar"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:src="@drawable/ic_model_avatar"
            android:scaleType="centerInside"
            android:backgroundTint="@color/surface_variant"
            android:layout_marginEnd="@dimen/space_12"
            android:contentDescription="Model avatar"
            android:importantForAccessibility="no" />

        <!-- Text content -->
        <LinearLayout
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:orientation="vertical"
            android:layout_gravity="center_vertical">

            <TextView
                android:id="@+id/tvChatName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAppearance="@style/TextAppearance.Qwen.TitleMedium"
                android:textColor="@color/on_surface"
                android:maxLines="1"
                android:ellipsize="end"
                tools:text="What is quantum computing?" />

            <TextView
                android:id="@+id/tvLastMessage"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:textAppearance="@style/TextAppearance.Qwen.BodySmall"
                android:textColor="@color/on_surface_variant"
                android:maxLines="2"
                android:ellipsize="end"
                android:layout_marginTop="@dimen/space_2"
                tools:text="Quantum computing uses qubits that can exist in multiple states simultaneously..." />
        </LinearLayout>

        <!-- Time + badge column -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:gravity="end"
            android:layout_marginStart="@dimen/space_8">

            <TextView
                android:id="@+id/tvTime"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="@style/TextAppearance.Qwen.LabelSmall"
                android:textColor="@color/on_surface_variant"
                tools:text="Today" />

            <TextView
                android:id="@+id/tvModelBadge"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textAppearance="@style/TextAppearance.Qwen.LabelSmall"
                android:textColor="@color/primary"
                android:background="@drawable/bg_model_badge"
                android:paddingHorizontal="@dimen/space_6"
                android:paddingVertical="@dimen/space_2"
                android:layout_marginTop="@dimen/space_4"
                android:visibility="visible"
                tools:text="4B"
                tools:visibility="visible" />
        </LinearLayout>
    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
